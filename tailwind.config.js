export const content = [
  "./src/**/*.{js,jsx,ts,tsx}", // Adjust this line as needed to match the file structure of your project
  // You can also include HTML files or any other files that may contain Tailwind CSS classes
];
export const theme = {
  extend: {},
};
export const plugins = [];
